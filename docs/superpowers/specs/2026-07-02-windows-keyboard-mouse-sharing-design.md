# Windows 局域网键鼠共享设计

日期：2026-07-02

## 目标

开发一个在局域网内让两台 Windows 电脑共用一套键盘和鼠标的工具。第一版聚焦核心体验：主控电脑使用真实键鼠，鼠标越过屏幕边缘后，键盘和鼠标控制权无缝切换到受控电脑；鼠标从受控电脑对应边缘返回后，控制权回到主控电脑。

第一版只支持 Windows 到 Windows，不做 Docker 开发环境，包含基础 GUI，不做剪贴板同步、文件拖拽、自动发现、账号系统或互联网远程控制。

## 技术栈

第一版采用 Rust 本机开发，输出一个 Windows GUI 程序。两台电脑运行同一个可执行文件，通过界面配置决定角色和连接目标。程序保留调试日志和少量命令行参数，但普通使用路径以 GUI 为主。

选择 Rust 的原因：

- 适合实现低延迟输入事件处理和网络通信。
- 内存安全，适合长期运行的后台工具。
- 可以通过 `windows` crate 调用 Windows API。
- 核心能力和 GUI 都可以放在同一个 Rust workspace 中，减少额外工具链。

核心依赖方向：

- Rust edition 2021 或更新版本。
- `eframe` / `egui`：实现第一版 Windows 图形界面。
- `windows` crate：调用 Windows hook、`SendInput`、显示器枚举、DPI 相关 API。
- `tokio`：TCP 网络通信、心跳、重连、异步任务。
- `serde` + `toml`：读取配置文件。
- `tracing` + `tracing-subscriber`：日志和调试输出。
- `bytes` 或手写轻量编解码：二进制协议打包。

第一版选择 `egui` 而不是 Tauri，是为了避免一开始引入 Node、前端构建链和 WebView 调试复杂度。GUI 主要承担配置和状态展示，不进入键鼠事件热路径。

## 运行模式

程序支持两个角色：

- `controller`：主控端，运行在插着真实键鼠的电脑上。负责捕获本机键鼠事件、判断边缘切换、维护控制状态、向受控端发送输入事件。
- `agent`：受控端，运行在被控制的电脑上。负责接收输入事件，并通过 Windows `SendInput` 注入到本机桌面。

第一版允许两台电脑都安装同一个程序，但一个配置为 `controller`，另一个配置为 `agent`。双向互控和多电脑拓扑不进入第一版。

## 配置

GUI 负责编辑配置，并持久化到 `config.toml`。配置文件仍然保留，方便调试、备份和手动修改。

主控端配置包含：

- 当前角色：`controller`
- 受控端 IP 和端口
- 受控端相对主控端的位置：`left`、`right`、`top`、`bottom`
- 边缘触发宽度，默认 2 像素
- 是否启用调试日志

受控端配置包含：

- 当前角色：`agent`
- 监听地址和端口
- 是否启用调试日志

## GUI 范围

第一版 GUI 聚焦配置和运行状态，不做复杂视觉设计。

主界面包含：

- 角色选择：`controller` 或 `agent`
- 主控端连接设置：受控端 IP、端口、相对位置
- 受控端监听设置：监听地址、端口
- 四向布局选择：`left`、`right`、`top`、`bottom`
- 边缘触发宽度设置
- 启动、停止、重连按钮
- 当前状态：未启动、等待连接、已连接、本机控制、远程控制、断线重连
- 延迟显示：最近 RTT、平均 RTT
- 简短事件日志：连接、断线、切换、权限错误、注入失败

GUI 线程只负责展示状态和发送控制命令。键鼠捕获、输入注入和网络发送不经过 GUI 线程，避免界面卡顿影响输入延迟。

## 架构

代码分为六个主要模块：

1. `ui`
   - 使用 `egui` / `eframe` 实现主窗口。
   - 编辑配置并保存到 `config.toml`。
   - 展示连接状态、延迟、事件日志和错误提示。
   - 向后台运行时发送启动、停止、重连等命令。

2. `config`
   - 读取和校验 `config.toml`。
   - 把字符串配置转换为强类型结构。

3. `protocol`
   - 定义网络消息类型和二进制编解码。
   - 负责输入事件、心跳、握手、错误通知的序列化。

4. `net`
   - 管理 TCP 连接。
   - 主控端负责连接受控端。
   - 受控端负责监听并接收连接。
   - 提供发送队列、心跳、断线检测和重连。

5. `win_input`
   - 主控端使用 `WH_MOUSE_LL` 和 `WH_KEYBOARD_LL` 捕获全局鼠标、键盘事件。
   - 主控端在远程控制激活时抑制本机输入事件。
   - 受控端使用 `SendInput` 注入鼠标和键盘事件。
   - 负责枚举虚拟桌面范围和 DPI 感知设置。

6. `control`
   - 控制状态机。
   - 判断是否进入远程控制。
   - 维护远程虚拟鼠标坐标。
   - 判断是否从远程桌面边缘返回。
   - 处理断线后的按键释放和状态恢复。

模块边界保持清晰：`ui` 不处理原始输入事件，`win_input` 不关心网络，`net` 不关心 Windows API，`control` 通过输入事件和连接状态驱动业务逻辑。

## 输入捕获

主控端通过 Windows 低级 hook 捕获输入：

- 鼠标：`WH_MOUSE_LL`
- 键盘：`WH_KEYBOARD_LL`

当控制状态为本机模式时，事件继续传递给 Windows，主控端只观察鼠标是否抵达配置方向的屏幕边缘。

当控制状态为远程模式时，主控端把鼠标移动、点击、滚轮、键盘按下和键盘释放事件转换为协议消息并发送给受控端，同时拦截这些事件，避免它们继续作用于主控电脑。

为避免程序崩溃或断线时出现按键卡住，主控端和受控端都维护当前按下的键集合。连接断开、退出程序或发生错误时，受控端会释放所有仍处于按下状态的按键和鼠标按钮。

## 输入注入

受控端使用 `SendInput` 注入事件。

鼠标移动第一版使用绝对坐标注入，坐标按受控端虚拟桌面矩形映射。鼠标按钮、滚轮和键盘事件按接收顺序注入。

限制：

- 普通权限程序通常不能向管理员权限窗口注入输入。
- 第一版要求两端程序以相同权限等级运行。
- 如果用户需要控制管理员窗口，应把两端都以管理员身份运行。后续产品化可再研究签名和 UIAccess。

## 屏幕边缘切换

程序把每台电脑的所有显示器视为一个虚拟桌面矩形。第一版支持四种相对位置：

- `left`
- `right`
- `top`
- `bottom`

进入远程控制时：

- 如果受控端在右侧，主控端鼠标到达主控虚拟桌面右边缘并继续移动时切换。
- 如果受控端在左侧，主控端鼠标到达左边缘并继续移动时切换。
- 如果受控端在上方，主控端鼠标到达上边缘并继续移动时切换。
- 如果受控端在下方，主控端鼠标到达下边缘并继续移动时切换。

切换坐标按比例映射：

- 左右切换时，把主控端 Y 坐标比例映射到受控端 Y 坐标。
- 上下切换时，把主控端 X 坐标比例映射到受控端 X 坐标。

远程控制期间，主控端维护一个远程虚拟坐标。收到真实鼠标移动 delta 后，更新远程虚拟坐标并发送给受控端。当远程虚拟坐标越过返回边缘时，退出远程控制，把主控端鼠标放回对应边缘附近，后续事件恢复作用于主控电脑。

## 通信方式

第一版使用一条常驻 TCP 连接。

选择 TCP 的原因：

- 键盘和鼠标按钮事件需要可靠、有序。
- 局域网内 TCP 延迟足够低，适合第一版验证核心体验。
- 实现复杂度低于 TCP + UDP 混合方案。

连接参数：

- 开启 `TCP_NODELAY`，减少 Nagle 算法带来的小包延迟。
- 输入事件使用二进制协议，避免热路径 JSON 编解码。
- 消息带类型、长度和递增序号，便于调试和检测异常。
- 心跳间隔默认 500ms。
- 超过 1500ms 未收到心跳或数据则认为断线。

主要消息：

- `Hello`：握手，交换协议版本和屏幕信息。
- `MouseMoveAbs`：远程绝对鼠标坐标。
- `MouseButton`：鼠标按下或释放。
- `MouseWheel`：滚轮。
- `Key`：键盘按下或释放。
- `Heartbeat`：心跳和延迟估算。
- `ReleaseAll`：释放所有按下状态。
- `Error`：协议错误或不可恢复问题。

## 降低延迟的策略

输入热路径不经过 UI，也不做磁盘写入。

主控端：

- hook 回调尽量短，只把事件投递到无锁或低开销队列。
- 网络发送任务单独运行，避免阻塞 hook。
- 鼠标移动事件可以轻量合并，只发送最新位置，目标间隔约 1 到 2ms。
- 鼠标按键、滚轮和键盘事件不合并，保证顺序和可靠性。

网络层：

- TCP 长连接。
- `TCP_NODELAY`。
- 二进制协议。
- 尽量减少消息分配和复制。

受控端：

- 网络接收和输入注入分离。
- 可以把同一批到达的输入事件合并为一次 `SendInput` 调用。
- 对鼠标移动使用最新坐标，避免旧移动事件堆积造成拖影。

预期体验目标：

- 局域网内鼠标移动端到端延迟尽量控制在 10ms 以内。
- 键盘和点击事件不丢失、不乱序。
- 断线后 1.5 秒内恢复本机控制并释放远程按键状态。

## 错误处理

必须处理的情况：

- 受控端未启动：主控端持续重连并提示状态。
- 网络断开：释放远程按键，退出远程控制，回到本机模式。
- 程序异常退出：尽量在退出处理里发送 `ReleaseAll`。
- 权限不足：检测输入注入失败次数，日志提示以相同权限运行。
- 协议版本不匹配：拒绝连接并输出清晰错误。

第一版日志输出到 GUI 事件面板和本地日志文件。日志中记录连接状态、角色、切换事件、断线事件、平均 RTT 和输入注入错误。调试运行时可以额外输出到控制台。

## 测试方案

可自动测试：

- 配置解析和错误提示。
- GUI 配置状态和保存逻辑。
- 协议编解码。
- 坐标比例映射。
- 控制状态机。
- 边缘进入和返回逻辑。
- TCP 客户端和服务端的本机回环通信。
- 断线时释放按键的状态转换。

需要人工验证：

- Windows 全局 hook 是否能捕获键鼠。
- 远程模式下本机输入是否被正确抑制。
- `SendInput` 是否能在受控端正确移动鼠标、点击、滚轮和键入。
- 上下左右四种布局是否能自然切换和返回。
- 两端权限不一致时是否能给出可理解的提示。
- GUI 启动、停止、重连和错误状态是否与后台状态一致。
- 高 DPI 和多显示器场景下坐标是否正确。

## 第一阶段实施顺序

1. 创建 `.gitignore`，覆盖 Rust、Windows 构建产物、日志、本地配置和编辑器临时文件。
2. 初始化 Rust workspace。
3. 建立 `egui` GUI 主窗口、后台运行时骨架和配置文件读取。
4. 实现角色选择、IP/端口、四向布局、边缘宽度和日志开关配置。
5. 实现 `protocol` 编解码和单元测试。
6. 实现坐标映射和控制状态机测试。
7. 实现 TCP 连接、心跳、重连和本机回环测试。
8. 把连接状态、RTT 和错误事件接入 GUI。
9. 实现受控端 `SendInput` 注入的最小验证。
10. 实现主控端全局 hook 捕获和远程模式输入抑制。
11. 联通主控端和受控端，实现鼠标边缘切换。
12. 补齐键盘、鼠标按钮、滚轮和断线释放逻辑。
13. 用两台 Windows 电脑验证四向布局、GUI 状态和延迟表现。

## 后续阶段

核心体验稳定后，再考虑：

- 托盘后台运行和开机自启。
- 自动发现和配对码。
- 剪贴板同步。
- 多电脑拓扑。
- 加密连接和设备信任。
- 程序签名、安装包和开机自启。

## 风险与取舍

第一版引入 GUI，但 GUI 只承担配置、启动停止、连接状态和日志展示。真正影响体验的输入捕获、输入抑制、输入注入、边缘切换和网络发送仍然在后台模块中完成，避免 GUI 线程进入热路径。

第一版选择 `egui` 而不是 Tauri，是为了减少额外依赖和安装负担。代价是界面会偏工具型，不追求复杂动画和高度定制视觉。等核心体验稳定后，再决定是否需要更完整的托盘程序、安装包或其他 GUI 框架。

第一版选择 TCP 而不是 UDP，是为了保证键盘和鼠标按钮事件可靠有序。局域网场景下，TCP 加 `TCP_NODELAY` 足以验证体验。只有当鼠标移动在实测中出现明显延迟或堆积时，才考虑引入 UDP 鼠标移动通道。

第一版只支持两台电脑，是为了让控制状态机保持简单。多电脑布局会引入图结构拓扑、路由和更多边界情况，适合在核心体验稳定后再做。
